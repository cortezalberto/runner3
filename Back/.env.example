# ===================================================================
# Environment Variables Template - Render.com Deployment
# ===================================================================
# Copy this file to .env and fill with your actual values
# ===================================================================

# Database Connection URL
# Render: Get this from Render PostgreSQL "Internal Database URL"
# Format: postgresql://user:password@hostname.render.com/database
DATABASE_URL=postgresql://playground:CHANGE_PASSWORD@your-db-host.render.com/playground

# Redis URL
# Render: Use Upstash Redis (Render doesn't offer free Redis)
# Get from: https://console.upstash.com/
# Format: redis://default:password@hostname.upstash.io:port
REDIS_URL=redis://default:CHANGE_PASSWORD@your-redis-host.upstash.io:6379

# CORS Configuration
# Add your Vercel frontend URL (without trailing slash)
CORS_ORIGINS=https://your-frontend.vercel.app

# Allow all origins (set to false in production for security)
CORS_ALLOW_ALL_ORIGINS=false

# ===================================================================
# NOTA IMPORTANTE: Docker NO disponible en Render
# ===================================================================
# La ejecución de código (Docker sandbox) NO funciona en Render.com
# El sistema solo mostrará problemas sin evaluar código enviado.
#
# Para ejecución de código completa, considera:
# - Railway.com (Hobby plan - $5/month, soporta Docker)
# - Fly.io (con Docker runtime)
# - DigitalOcean App Platform (con soporte Docker)
# - VPS propio con Docker instalado
# ===================================================================
